#include <stdio.h>

/**
 * main - Program that will print all single digit starting from 0
 * Return: Always 0
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
